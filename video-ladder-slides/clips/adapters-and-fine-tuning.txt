adapters-and-fine-tuning: 24 slides, 72 clips, 1253 words, about 7.5 min at Scout7's 2.8 words a second
  c001 CAMERA (22) This video is about the first change that touches the model itself. You teach it with examples of the task done well.
  c002 ad-intro b1 (19) This video covers adapters and fine-tuning. The first three steps of the ladder change the system around the model.
  c003 ad-intro b1 (16) The model itself stays the same. This is the first option that changes the model itself.
  c004 ad-intro b2 (10) First, we look at what training on examples really does.
  c005 ad-intro b2 (15) Then we see three ways to do it and what you need before you start.
  c006 ad-intro b2 (18) Next, we see how to move work to a smaller model. Last comes the test for moving on.
  c007 ad-22 b1 (12) Until now, the model itself stayed the same. Now you change it.
  c008 ad-22 b1 (20) You collect many examples of the task done well. Each example is an input paired with the answer you wanted.
  c009 ad-22 b2 (24) The model is trained on these pairs until it answers more like them. Each example shows the model the right answer to aim for.
  c010 ad-22 b2 (17) Training a model this way is called supervised fine-tuning. From here on, we will just say fine-tuning.
  c011 ad-02 b1 (18) Fine-tuning is the right tool for two jobs. It makes the model behave the same way every time.
  c012 ad-02 b1 (10) And it can move work to a smaller, cheaper model.
  c013 ad-02 b2 (15) It is the wrong tool for teaching the model facts. The next slides show why.
  c014 ad-03 b1 (19) To see why, look at what training does inside the model. A model is a huge set of numbers.
  c015 ad-03 b1 (16) These numbers are called weights. Training shows the model an input and the answer you wanted.
  c016 ad-03 b2 (22) It measures how far the model's own answer is from that. Then it nudges the weights a little to close the gap.
  c017 ad-03 b3 (18) This repeats over thousands of examples. Slowly, the model's usual behaviour moves toward the pattern the examples share.
  c018 ad-04 b1 (19) Training rewards answers that look like your examples. So the model learns their structure, their tone and their confidence.
  c019 ad-04 b2 (18) Nothing in the process stores the facts behind them. A few thousand examples cannot carry a million facts.
  c020 ad-04 b2 (15) So a fine-tuned model can sound just like your examples and still get details wrong.
  c021 ad-14 b1 (16) So where should facts come from? Look them up at the moment the model needs them.
  c022 ad-14 b1 (13) Then pass them to the model with the question. This is called retrieval.
  c023 ad-14 b2 (12) Facts baked into the weights cannot be traced back to a source.
  c024 ad-14 b3 (18) They cannot be updated or deleted either. Facts that are looked up can be traced, updated and deleted.
  c025 ad-05 b1 (16) Fine-tuning also has a side effect. The weights being nudged also hold the model's general skills.
  c026 ad-05 b1 (23) If you push them hard toward your task, they drift. The model can lose skills it had before. This is called catastrophic forgetting.
  c027 ad-06 b1 (20) Now for how fine-tuning is actually done. There are three main ways. The first changes every weight in the model.
  c028 ad-06 b2 (20) This is called full fine-tuning. It has the most power to change behaviour. It also needs the most computing power.
  c029 ad-06 b2 (13) And you end up with a whole new model to store and run.
  c030 ad-07 b1 (23) The second way leaves the model's own weights locked. The change fine-tuning makes can usually be captured in two small tables of numbers.
  c031 ad-07 b2 (23) So only those two small tables are trained. When the model runs, their effect is added on top. This method is called LoRA.
  c032 ad-23 b1 (13) A whole model takes gigabytes of space. What LoRA produces takes only megabytes.
  c033 ad-23 b2 (14) This small add-on is called an adapter. You can keep one shared model running.
  c034 ad-23 b2 (18) Then you swap in a different adapter for each task. So one model can do many different jobs.
  c035 ad-08 b1 (19) The third way builds on LoRA. While the adapter trains, the locked model is stored in a compressed form.
  c036 ad-08 b1 (15) That saves so much computer memory that one powerful chip can adapt a large model.
  c037 ad-08 b2 (17) The adapter itself is not compressed. The quality stays close to LoRA. This method is called QLoRA.
  c038 ad-09 b1 (16) Changing fewer weights sounds safer. You might expect an adapter to protect the model's other skills.
  c039 ad-09 b1 (18) But published research found that LoRA does not protect them. If forgetting matters for your use, measure it.
  c040 ad-12 b2 (21) Whichever way you choose, first check that fine-tuning fits your problem. Here is the typical case. The answers are already correct.
  c041 ad-12 b4 (23) But their structure and tone still vary from one answer to the next. Adding example answers to the instructions has not fixed it.
  c042 ad-10 b1 (17) You need a few thousand examples. The model learns exactly what they teach. That includes their mistakes.
  c043 ad-10 b2 (18) So their quality matters more than anything else. Before training, write a set of tests and lock them.
  c044 ad-10 b2 (20) Include tests for skills that should not change. If you only test the new task, forgetting can slip through unnoticed.
  c045 ad-10 b3 (18) You also need a plan to run the result. An adapter needs its original model to run on.
  c046 ad-10 b3 (13) When that model is replaced, the work must be redone and tested again.
  c047 ad-24 b1 (16) The training itself has to happen somewhere. You do not have to use your own machines.
  c048 ad-24 b1 (15) Some providers run fine-tuning for you as a service. This is called a managed option.
  c049 ad-24 b1 (10) You send the examples and get back the trained result.
  c050 ad-11 b1 (17) These managed options changed in 2026. This fact changes often. It is correct as of August 2026.
  c051 ad-11 b2 (15) OpenAI is closing the fine-tuning that customers set up themselves. It is closing in stages.
  c052 ad-11 b3 (21) Remaining customers can start no new jobs after 6 January 2027. Managed fine-tuning on the major cloud platforms is still available.
  c053 ad-11 b4 (22) Some models let you download their weights and run them yourself. These are called open-weight models. Adapters on these models are unaffected.
  c054 ad-15 b1 (23) Now back to one of the two jobs fine-tuning is good for. The biggest and most capable models cost the most to run.
  c055 ad-15 b2 (18) These are called frontier models. Fine-tuning can move a task from a frontier model to a small one.
  c056 ad-15 b2 (14) A small fine-tuned model can do the task for a fraction of the cost.
  c057 ad-15 b2 (13) It can run on your own servers. Your data then stays with you.
  c058 ad-16 b1 (20) One published test compared a small fine-tuned model with a frontier model. The frontier model was given careful instructions instead.
  c059 ad-16 b1 (21) The task was putting items into the right category. The small model won by a wide margin at far lower cost.
  c060 ad-16 b2 (15) But that is only one test. Prove it on your own tests before you switch.
  c061 ad-18 b1 (18) So when is this step done? Move on when the format and behaviour of the answers are right.
  c062 ad-18 b1 (21) At that point, what is still wrong is judgement or knowledge. Where you go next depends on which one it is.
  c063 ad-19 b1 (21) Say the answers look right, but the reasoning is wrong. And say a program can check whether each outcome is right.
  c064 ad-19 b1 (18) Then go to reinforcement fine-tuning. There, the model tries the task and learns from the scores it gets.
  c065 ad-20 b1 (17) Say the model still misreads the language and documents of your field. Then go to continued pretraining.
  c066 ad-20 b1 (12) There, the model keeps learning from large amounts of your own text.
  c067 ad-21 b1 (14) Say the quality is right, but it costs too much or runs too slowly.
  c068 ad-21 b1 (17) Then go to distillation. There, a large model trains a small one to do the same work.
  c069 ad-recap b1 (22) Fine-tuning trains a model on examples of a task done well. It makes behaviour consistent. It does not teach the model facts.
  c070 ad-recap b2 (19) Facts should still be looked up when the model needs them. Training can make the model forget other skills.
  c071 ad-recap b2 (20) Adapters do not stop this. So measure it. An adapter is a small add-on you can swap for each task.
  c072 CAMERA (14) Fine-tuning can also move work to a smaller, cheaper model. Next up is distillation.
