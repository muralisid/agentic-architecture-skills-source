reinforcement-fine-tuning: 17 slides, 59 clips, 1070 words, about 6.4 min at Scout7's 2.8 words a second
  c001 CAMERA (20) Most training shows a model examples of right answers. Training a model further on examples like these is called fine-tuning.
  c002 rf-01 b1 (19) But good judgement is hard to show with examples. There is a kind of fine-tuning that can teach it.
  c003 rf-intro b1 (22) The ladder is the order in which to try changes to an agent. The early steps change the system around the model.
  c004 rf-intro b2 (24) The later steps change the model itself. Reinforcement fine-tuning is one of those later steps. First, we look at how training with scores works.
  c005 rf-intro b4 (17) Then we see how the score can go wrong. Next comes the warning sign to watch for.
  c006 rf-intro b6 (20) Then we see what this training can fix and what it cannot. Last, we look at where to go next.
  c007 rf-20 b1 (18) First, how this training works. Ordinary fine-tuning pulls a model towards fixed right answers. This kind works differently.
  c008 rf-20 b1 (14) The model tries the task in its own way. Each attempt gets a score.
  c009 rf-20 b1 (13) The model is pulled towards whatever scores well. This is called reinforcement fine-tuning.
  c010 rf-02 b1 (16) Here is the loop, step by step. First, the model makes an attempt at the task.
  c011 rf-02 b2 (16) Next, something checks the attempt and gives it a score. This checker is called a grader.
  c012 rf-02 b3 (16) Then training makes the behaviour behind high scores more likely. The loop runs again and again.
  c013 rf-03 b1 (18) This freedom to search is where the power comes from. On some tasks, good answers vary a lot.
  c014 rf-03 b1 (22) You cannot list them all as examples. On tasks like these, the model can find ways of working that no example showed.
  c015 rf-21 b1 (21) This training can also work with few tasks. The company that sells it reported gains from about a hundred graded tasks.
  c016 rf-21 b1 (10) Ordinary fine-tuning on that few examples made the model worse.
  c017 rf-21 b1 (23) A result a seller reports about its own product is called a vendor result. So this one is flagged as a vendor result.
  c018 rf-04 b1 (22) The same freedom to search also brings a danger. The model chases the score. It does not know what you really meant.
  c019 rf-04 b2 (15) Say the score and your real goal differ somewhere. The model will find that gap.
  c020 rf-04 b3 (17) It gets far more attempts than you spent building the grader. This is not cheating on purpose.
  c021 rf-04 b3 (22) Shortcuts that score well simply become more likely. Getting a high score without doing the real job is called gaming the grader.
  c022 rf-05 b1 (20) Here are four common ways a grader gets gamed. The first is surface style. Say tidy, confident answers score well.
  c023 rf-05 b2 (16) Then the model learns to look tidy and confident, right or wrong. The second is length.
  c024 rf-05 b3 (23) If long answers score well, expect the model to pad them out. The third is using another AI model to give the score.
  c025 rf-05 b3 (15) A model used this way is called a judge. The judge has its own biases.
  c026 rf-05 b4 (21) It tends to favour some kinds of answer. That becomes part of the score. The fourth is checks that miss things.
  c027 rf-05 b4 (17) Say the grader checks three things. The model gets good at those three and neglects the rest.
  c028 rf-06 b1 (18) Judges have one well-known bias. Related models from the same maker form a group called a model family.
  c029 rf-06 b1 (20) A model tends to prefer answers in the style of its own family. We saw this in our own experiments.
  c030 rf-06 b1 (20) A judge from the same family found a difference. That difference vanished when a judge from another family checked it.
  c031 rf-12 b1 (21) Gaps can also hide in old records. It is tempting to score a model against decisions people made in the past.
  c032 rf-12 b1 (16) But those decisions include people's own mistakes. Say a decision to act gets checked again later.
  c033 rf-12 b2 (20) A decision to do nothing is almost never looked at again. So a wrong decision to do nothing stays hidden.
  c034 rf-12 b2 (14) A grader built on these records would teach the model the same blind spots.
  c035 rf-07 b1 (16) So when is a grader safe? It is safe when a program can check the result.
  c036 rf-07 b2 (17) That program must not share the model's blind spots. Think of code that must pass its tests.
  c037 rf-07 b2 (17) Or a figure that must match its source document. Or a schedule that must meet every rule.
  c038 rf-07 b3 (13) Some quality comes down to taste or tone. No program can check that.
  c039 rf-07 b3 (13) There, the grader is only a stand-in. And a stand-in will be gamed.
  c040 rf-08 b1 (11) So how do you spot a grader that is being gamed?
  c041 rf-08 b1 (20) Say the model scores higher than a careful person on the same task. Do not treat that as a win.
  c042 rf-08 b1 (21) It means the model learned the scoring rules instead of the job. The score works like a reward for the model.
  c043 rf-08 b1 (17) So this failure is called reward hacking. When it happens, treat the grader as the broken part.
  c044 rf-09 b1 (16) So look after the grader like any important software. Keep track of every change to it.
  c045 rf-09 b1 (13) Have someone review each change. Test it. And give it a named owner.
  c046 rf-09 b1 (22) A fault in the grader trains the same fault into the model. That model gives no warning. It just scores very well.
  c047 rf-17 b1 (18) Now, what can this training fix? It fixes judgement on tasks where a program can check the result.
  c048 rf-17 b1 (19) It does not fix format, tone or style. Those are easy to show with examples. Ordinary fine-tuning handles them.
  c049 rf-17 b2 (17) It also fixes reasoning over many steps. But it cannot add knowledge the model does not have.
  c050 rf-17 b2 (12) It changes how the model behaves. It does not store new facts.
  c051 rf-10 b1 (23) There are published gains from this training in specialist fields. They come from the company that sells it. They describe its own customers.
  c052 rf-10 b2 (24) So they are vendor results. They cover tax analysis and a medical task scored against doctors. They also cover pulling references out of documents.
  c053 rf-18 b1 (24) The last part is where to go next. Say the model's judgement is now right. But it still misreads the material in your field.
  c054 rf-18 b2 (24) Then what is missing is knowledge of your field. You can add it by training the model further on plain text from your field.
  c055 rf-18 b2 (21) This is called continued pretraining. And once quality is proven, a smaller model can learn to do the job more cheaply.
  c056 rf-recap b1 (19) Here is what to remember. Before you start, you need a grader that only the real job can satisfy.
  c057 rf-recap b3 (21) This training moves the model towards whatever scores well. So any gap between the score and your goal will be found.
  c058 rf-recap b5 (15) If the model beats a careful person, check the grader first. This training fixes judgement.
  c059 CAMERA (11) It does not add knowledge. Next, we look at continued pretraining.
