context: 22 slides, 81 clips, 1469 words, about 8.7 min at Scout7's 2.8 words a second
  c001 CAMERA (23) Step two is about what an AI model gets to see. Everything the model can see when it answers is called its context.
  c002 cx-01 b1 (13) To get there, documents are cut up, turned into numbers, stored and searched.
  c003 cx-01 b1 (13) In our own measurements, these choices changed the answer more than anything else.
  c004 cx-intro b1 (23) Some changes to an AI system are worth trying before others. The first three are tried in order, one step at a time.
  c005 cx-intro b1 (15) This order is called the ladder. Step one was the instructions you give the model.
  c006 cx-intro b2 (16) This is step two, context. First, we follow a document on its way to the model.
  c007 cx-intro b2 (21) Then we see what goes wrong when one document covers many topics. We also see how to test a fix fairly.
  c008 cx-intro b3 (19) After that, we look at searching for exact details. Then we look at who chooses and owns the documents.
  c009 cx-intro b3 (19) Last, we look at what is kept for later use. The video ends with the test for moving on.
  c010 cx-02 b1 (24) A system that uses a model to do a job is called an agent. To answer questions, an agent often needs facts from documents.
  c011 cx-02 b2 (20) Let us follow one document on its way to the model. First, the file is read and turned into text.
  c012 cx-02 b3 (17) Then the text is cut into smaller pieces. Each piece is turned into a list of numbers.
  c013 cx-02 b4 (15) The numbers are stored so they can be searched quickly. Then a question comes in.
  c014 cx-02 b4 (24) The best pieces are found and passed to the model. Each stage has its own name. We will take them one at a time.
  c015 cx-20 b1 (13) The first stage turns the file into plain text. This is called parsing.
  c016 cx-20 b2 (22) Good parsing keeps the structure, like the rows and columns of a table. Say a table is flattened into one long sentence.
  c017 cx-20 b2 (23) The numbers lose the labels that went with them. No later stage can put that back. So keep checking samples of the parsing.
  c018 cx-21 b1 (17) The second stage cuts the text into pieces that search can find. These pieces are called chunks.
  c019 cx-21 b2 (20) A chunk that is too large mixes several topics. A chunk that is too small loses the text around it.
  c020 cx-21 b3 (24) Without that text, it can lose its meaning. No single size works for every set of documents. So the size has to be tested.
  c021 cx-22 b1 (19) The third stage turns each piece into a list of numbers. The numbers stand for what the piece means.
  c022 cx-22 b2 (20) This list of numbers is called a vector. Think of each vector as a point on a map of meaning.
  c023 cx-22 b2 (15) Pieces that mean similar things sit close together. Pieces about different things sit far apart.
  c024 cx-23 b1 (18) The fourth stage stores the vectors so they can be searched fast. This store is called an index.
  c025 cx-23 b2 (17) The index also records who is allowed to see each document. The search uses that record later.
  c026 cx-24 b1 (20) The last stage starts when someone asks a question. The question is turned into a vector in the same way.
  c027 cx-24 b1 (11) The search finds the pieces whose vectors sit nearest to it.
  c028 cx-24 b2 (21) Those pieces are passed to the model along with the question. Finding and passing on the right pieces is called retrieval.
  c029 cx-13 b1 (20) Retrieval needs one more rule. Not everyone is allowed to see every document. One way is to search everything first.
  c030 cx-13 b1 (15) Then remove what the person may not see. The other way narrows the search first.
  c031 cx-13 b2 (14) It only searches what that person is allowed to see. Filtering afterwards leaks information.
  c032 cx-13 b2 (18) Even the number of results can show that a hidden document exists. Filtering first gives no such hint.
  c033 cx-04 b1 (16) Now to a mistake that catches almost everyone. Say a whole document gets just one vector.
  c034 cx-04 b1 (21) That vector is roughly the average of everything in the document. That is fine if the document is about one thing.
  c035 cx-04 b2 (18) But documents at work are rarely about one thing. Picture a long document that covers four different topics.
  c036 cx-04 b2 (17) Its one vector sits in the middle of all four. It is close to none of them.
  c037 cx-04 b3 (17) Now ask about just one of those topics. The document comes back as only a weak match.
  c038 cx-05 b1 (19) We tested this in our own experiments. We made each document cover more topics, from one up to ten.
  c039 cx-05 b2 (19) Everything else stayed the same. Search quality fell sharply. Then we gave each document several vectors instead of one.
  c040 cx-05 b2 (21) That brought search quality almost all the way back. We repeated this on two different machines. The result was the same.
  c041 cx-06 b1 (18) There was a problem with that result. Several vectors give a document more chances to match a question.
  c042 cx-06 b2 (19) So some of the gain might come just from having more pieces. We ran a fairer test to check.
  c043 cx-06 b2 (12) We shaped pieces around what each part of a document was for.
  c044 cx-06 b2 (18) We compared them with the same number of plain chunks. On real text, our shaped pieces did worse.
  c045 cx-07 b1 (18) So test any new design against a plain version. Give the plain version the same number of pieces.
  c046 cx-07 b1 (11) A fair comparison like this one is called a matched control.
  c047 cx-07 b2 (18) Your design has to beat it on your own questions. Otherwise you are only measuring the extra pieces.
  c048 cx-08 b1 (21) So far, search has worked by meaning, using vectors. Search by meaning is poor at exact details, like a phone number.
  c049 cx-08 b1 (14) Another kind of search looks for the exact text. This is called keyword search.
  c050 cx-08 b2 (22) In our tests on exact details, keyword search scored far higher. Search by meaning does not show an error when it misses.
  c051 cx-08 b3 (21) It returns something that looks right instead. So run both kinds of search and merge the results. It costs almost nothing.
  c052 cx-14 b1 (11) Now think about which documents belong in the search at all.
  c053 cx-14 b2 (21) The whole set of documents that can be searched is called the corpus. Adding more documents can make answers less accurate.
  c054 cx-14 b2 (23) Published work showed accuracy coming back once the corpus was narrowed to the subject. A few good documents usually work better than many.
  c055 cx-16 b1 (11) Someone has to choose and tidy the documents in the corpus.
  c056 cx-16 b1 (14) This can be done by people, by software with spot checks, or by both.
  c057 cx-16 b2 (16) But one person or team must still own the corpus. A real legal case shows why.
  c058 cx-16 b2 (23) In Moffatt versus Air Canada, the company's chatbot went against its own policy. The company was held responsible for what the chatbot said.
  c059 cx-25 b1 (19) Documents are not the only thing the model sees. An agent can also keep notes from its earlier work.
  c060 cx-25 b2 (18) What it keeps is called memory. When memory is used, it reaches the model just like a document.
  c061 cx-25 b2 (15) So it needs the same care as a document. It also brings some extra duties.
  c062 cx-09 b1 (15) Memory can last for different lengths of time. Some lasts only for the current conversation.
  c063 cx-09 b2 (18) Some lasts for a stretch of work of hours or days. Both of these are ordinary engineering choices.
  c064 cx-09 b3 (18) Memory kept after the work ends is different. It becomes a record. It needs someone who owns it.
  c065 cx-09 b3 (22) It also needs consent to keep it. And it needs rules for how long it is kept and when it is deleted.
  c066 cx-10 b1 (19) Four rules follow from published evidence. First, keep the original text. Then add labels to help search find it.
  c067 cx-10 b2 (19) In published tests, swapping the text for short facts or summaries made results worse. Second, record when something happened.
  c068 cx-10 b2 (22) Keep that apart from the time it was saved. Then a fact that has been replaced can be marked out of date.
  c069 cx-10 b3 (17) Third, check every new memory before it is kept. It carries its source and waits for review.
  c070 cx-10 b3 (18) In published work, planting bad entries in a tiny part of a memory store made most attacks succeed.
  c071 cx-10 b4 (22) Fourth, topics are only labels that help search. They are not the memory itself. They can be rebuilt from the kept text.
  c072 cx-17 b1 (22) So when is step two done? There are three signs. First, your way of splitting and storing documents beats a matched control.
  c073 cx-17 b2 (19) It has to win on your own questions. Second, everything the agent keeps has a source and a date.
  c074 cx-17 b3 (24) It has also passed a check. Third, the agent now finds the right facts. What still goes wrong is what it does with them.
  c075 cx-18 b1 (12) Here is what that looks like. The agent finds the right documents.
  c076 cx-18 b1 (14) Then it draws the wrong conclusion, takes the wrong action, or stops too early.
  c077 cx-18 b2 (15) That is not a context problem. It belongs to step three, tools and the loop.
  c078 cx-recap b1 (21) Here is what to remember from step two. A document is parsed, cut into chunks, turned into vectors, indexed and retrieved.
  c079 cx-recap b1 (13) One vector for a document about many topics matches each topic only weakly.
  c080 cx-recap b2 (19) Test any new design against a matched control on your own questions. Run keyword search alongside search by meaning.
  c081 CAMERA (20) Give everything the agent keeps a source, a date and a check. Next is step three, tools and the loop.
