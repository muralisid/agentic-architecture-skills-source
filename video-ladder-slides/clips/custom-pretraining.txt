custom-pretraining: 24 slides, 84 clips, 1492 words, about 8.9 min at Scout7's 2.8 words a second
  c001 CAMERA (15) At the top of the market, two very different things are sold in similar words.
  c002 ca-01 b1 (12) This chapter helps you tell which one is in front of you.
  c003 ca-intro b1 (19) This is the last chapter of the ladder. It is one of five ways to change the model itself.
  c004 ca-intro b2 (15) Very few organisations ever need it. First, we look at two offers that sound alike.
  c005 ca-intro b2 (14) Then we look at path one. That is building your own model from nothing.
  c006 ca-intro b3 (17) Next is path two. It is a different kind of model for data that is not text.
  c007 ca-intro b3 (13) Last, we see how this links back to the rest of the ladder.
  c008 ca-20 b1 (19) Start with how most AI models are made. They are trained on huge amounts of text from every subject.
  c009 ca-20 b1 (11) Then other models and products are built on top of them.
  c010 ca-20 b2 (19) A large model like this is called a foundation model. Most teams start from one that someone else built.
  c011 ca-02 b1 (23) Here are the two offers. One is to build your own foundation model from nothing. The other is a different kind of model.
  c012 ca-02 b2 (15) It is built for data that is not text. The first comes down to cost.
  c013 ca-02 b2 (17) It is almost never the answer. The second is real specialist work. It has a clear use.
  c014 ca-03 b1 (23) Start with path one. A model is a huge set of numbers. Training slowly adjusts those numbers. Here, the numbers start out random.
  c015 ca-03 b2 (23) The model then reads trillions of small pieces of words. This huge first training is called pretraining. Nothing comes from an existing model.
  c016 ca-03 b3 (20) So everything it knows must come from your own text. Last, it is tuned so that people can use it.
  c017 ca-04 b1 (20) Path one costs what only the biggest AI labs spend. It needs computing power costing tens of millions and more.
  c018 ca-04 b2 (15) It also needs a research team that stays in place. Building takes a long time.
  c019 ca-04 b3 (20) The field keeps moving while you build. Meanwhile, open and paid models keep getting better. Someone else pays for that.
  c020 ca-05 b1 (20) Path one does make sense in two cases. In the first, a national rule bans using any model from outside.
  c021 ca-05 b2 (16) This is called a sovereignty rule. In the second, you hold a huge amount of text.
  c022 ca-05 b2 (21) It is so unlike public text that existing models get it wrong. This case also needs the money and the team.
  c023 ca-06 b1 (10) One question settles most plans to build your own model.
  c024 ca-06 b1 (20) What if you took an existing model and kept training it on your own text? That is called continued pretraining.
  c025 ca-06 b2 (16) It gives most of what people want from their own model. It uses the same text.
  c026 ca-06 b2 (15) It costs a small fraction as much. And someone else keeps improving the model underneath.
  c027 ca-07 b1 (22) If you do take path one, one finding may help. It is about when to mix in text from your own field.
  c028 ca-07 b2 (13) One study mixed a little of that text in from the very start.
  c029 ca-07 b2 (12) That beat training on public text first and adding your text later.
  c030 ca-07 b3 (18) The study was published by a company that sells help with training data. It also used small models.
  c031 ca-07 b3 (17) It can guide how you run path one. It does not tell you whether to take it.
  c032 ca-08 b1 (18) Now for path two. The models on this ladder read text as a line of small word pieces.
  c033 ca-08 b2 (18) Some data is not like that at all. Think of records linked to each other, or supply chains.
  c034 ca-08 b3 (16) Think of streams of sensor readings, or molecules. None of this is a line of words.
  c035 ca-08 b4 (20) None of the earlier steps on this ladder can change that. Much of this data is things joined by links.
  c036 ca-08 b4 (11) A set of things joined by links is called a graph.
  c037 ca-21 b1 (21) For data like this, the model itself has to be different. It has to learn from who is linked to whom.
  c038 ca-21 b2 (20) The most common choice is called a graph neural network. In this chapter, we just call it a graph model.
  c039 ca-22 b1 (22) Here is how a graph model works, one step at a time. Take one thing in the graph, such as one record.
  c040 ca-22 b2 (19) It is called a node. Each node starts with a list of numbers. The numbers describe that one thing.
  c041 ca-09 b1 (14) Now the model gets to work. It starts from each node's list of numbers.
  c042 ca-09 b2 (18) A node's neighbours are the nodes it links to. Each node updates its numbers using its neighbours' numbers.
  c043 ca-09 b3 (16) One round of this is called a layer. The model runs several layers, one after another.
  c044 ca-09 b4 (21) After two layers, a node knows about its neighbours' neighbours. Last, the model reads each node's numbers to make a prediction.
  c045 ca-09 b4 (15) It learns from who connects to whom. A model that reads documents cannot see that.
  c046 ca-10 b1 (18) This way of working has two known problems. Each layer mixes a node's numbers with its neighbours' numbers.
  c047 ca-10 b2 (21) Stack enough layers, and nearby nodes end up with the same numbers. Then the model can no longer tell them apart.
  c048 ca-10 b2 (19) This is called over-smoothing. It is why graph models in real use often have only two or three layers.
  c049 ca-11 b1 (19) The second problem pulls the other way. Information from a far node must pass through many nodes to arrive.
  c050 ca-11 b2 (22) The number of nodes on the way grows very fast with distance. But each node holds only a fixed-size list of numbers.
  c051 ca-11 b2 (17) So far information gets squeezed through narrow links. Much of it is lost. This is called over-squashing.
  c052 ca-12 b1 (23) So the two problems pull against each other. More layers let information travel further. That eases over-squashing. But more layers make over-smoothing worse.
  c053 ca-12 b2 (22) So turning one setting up does not fix both. People who have really done this work change the design of the model.
  c054 ca-12 b3 (19) A model's design is called its architecture. Watch how someone handles this. It shows whether they have real experience.
  c055 ca-13 b1 (16) There are three more problems worth listening for. First, the learned numbers can stop being distinct.
  c056 ca-13 b1 (17) Or learning can fade out in the deeper layers. Then parts of the model quietly stop learning.
  c057 ca-13 b2 (16) Second, a huge graph may not fit in memory. So it must be cut into parts.
  c058 ca-13 b2 (15) Every cut breaks real links. So the model trains on a graph with links missing.
  c059 ca-13 b3 (19) Third, the training may be spread across many machines. Then training no longer behaves the way the textbooks describe.
  c060 ca-14 b1 (19) So when does path two make sense? It fits when what you predict depends on how things link together.
  c061 ca-14 b2 (21) It does not sit inside any one document. The data must also be big enough for these problems to really matter.
  c062 ca-14 b3 (19) This situation is easy to spot. The rest of this ladder is about AI systems that carry out tasks.
  c063 ca-14 b3 (13) These systems are called agents. Path two has little to do with them.
  c064 ca-16 b1 (19) Now, how does this connect back to agents? The graph model is not the agent. It does not chat.
  c065 ca-16 b1 (22) It runs on its own as a scoring system. That system has its own checks on whether the model can be trusted.
  c066 ca-16 b2 (13) The agent reaches the score by asking a small program to fetch it.
  c067 ca-16 b2 (20) A program like this is called a tool. It returns the score and the part of the graph behind it.
  c068 ca-16 b3 (14) The agent uses them as evidence in its draft. A person makes the decision.
  c069 ca-17 b1 (19) This also means two different teams. Every earlier step on this ladder starts from a model that already exists.
  c070 ca-17 b2 (16) An engineering team builds around it. The graph model needs a research team that builds models.
  c071 ca-17 b3 (23) That team has its own way of checking and approving models. These are two different kinds of work. They meet at the tool.
  c072 ca-17 b3 (20) Giving the graph model to the agent team is a mistake. So is giving the agent to the graph team.
  c073 ca-18 b1 (22) Before building a custom graph model, try the simpler options. First, work out a few simple measures of the graph by hand.
  c074 ca-18 b2 (20) Then feed them to a simple model. Second, try a ready-made graph model. Keep it only a few layers deep.
  c075 ca-18 b3 (17) Third, try rules for patterns you already know. Each of these options gets a fair run first.
  c076 ca-23 b1 (14) Then compare them fairly. Use past cases where you already know the right answer.
  c077 ca-23 b2 (18) These are called labelled cases. The custom model must beat the simple options on your own labelled cases.
  c078 ca-23 b2 (13) This is the same fair test used at the start of this ladder.
  c079 ca-19 b1 (20) The same habit applies at every step of this ladder. Find out what is really failing before you spend money.
  c080 ca-19 b1 (12) Never assume a bigger version of what you have will fix it.
  c081 ca-recap b1 (24) Here is what to remember. Building your own foundation model almost never pays. First ask what continued pretraining on the same text would give.
  c082 ca-recap b2 (18) Data made of linked things may need a graph model. That model is a tool the agent calls.
  c083 ca-recap b3 (16) A different team builds it. At every step, find out what is failing before you spend.
  c084 CAMERA (18) The rule from the start of this ladder still holds. Change the system before you change the model.
