index: 34 slides, 92 clips, 1661 words, about 9.9 min at Scout7's 2.8 words a second
  c001 CAMERA (15) When an AI system is not good enough, there are eight things you can change.
  c002 ov-01 b1 (16) They are not equally sensible to try. This video shows the order worth trying them in.
  c003 ov-intro b1 (15) Here is what this video covers. We start with two simple words you will need.
  c004 ov-intro b3 (19) Then we look at why the order of changes matters. Next, three changes to the system around the AI.
  c005 ov-intro b5 (20) Then five changes to the AI itself. Last, how to measure every change. That is how you know what worked.
  c006 ov-model b1 (17) At the heart of any AI system is a program that reads text and writes text back.
  c007 ov-model b1 (15) You give it words, and it answers with words. This program is called the model.
  c008 ov-agent b1 (20) A model on its own only writes answers. Often we want more. We want it to finish a whole task.
  c009 ov-agent b2 (23) So we build a system around the model. It lets the model take actions, one step after another. This is called an agent.
  c010 ov-02 b1 (18) Now you know what a model and an agent are. So why does the order of changes matter?
  c011 ov-02 b2 (21) When an agent fails, a common first reaction is to blame the model. People say the model is not good enough.
  c012 ov-02 b2 (20) But that does not say what is actually wrong. It is usually said before anyone has measured what is failing.
  c013 ov-03 b1 (23) The usual answer is to change the model itself. You feed it new examples, and it learns from them. This is called training.
  c014 ov-03 b3 (22) Training too early costs a lot of money. It also makes the system heavier. And it can tie you to one model.
  c015 ov-03 b4 (18) You now have a trained model to look after. And the real fault is still sitting there underneath.
  c016 ov-04 b1 (18) Published work points the other way. Better wording and better information for the model are cheap to try.
  c017 ov-04 b2 (21) They keep finding gains that teams thought were used up. The expensive changes often fix something that was never the problem.
  c018 ov-07 b1 (19) We saw why jumping to training too early is costly. Now, here are all eight things you can change.
  c019 ov-07 b2 (16) The first three change the system around the model. The model itself stays exactly the same.
  c020 ov-07 b3 (12) The last five change the model itself. These are the expensive ones.
  c021 ov-08 b1 (12) The first three work like a ladder. You climb them in order.
  c022 ov-08 b1 (16) Finish each step before the next one is worth trying. We call this group Deck A.
  c023 ov-09 b1 (22) Step one is the instructions. You write words that tell the model what the task is. Those words are called the prompt.
  c024 ov-09 b2 (24) Keep improving the prompt. Measure how often the answers are right. Move on when that climbs for a week of work, then goes flat.
  c025 ov-10 b1 (16) Step two is context. Everything that reaches the model along with the task is called context.
  c026 ov-10 b2 (23) It includes your documents, and what the agent remembers from before. First, your way of preparing documents must beat a plain, simple version.
  c027 ov-10 b2 (24) Test both on the same cases. That keeps it fair. Then move on when the failures left are about taking action, not missing knowledge.
  c028 ov-loop b2 (22) Before step three, one more idea. An agent works in rounds. First, it picks an action. Then it looks at the result.
  c029 ov-loop b3 (18) Then it decides what to do next, and goes round again. This repeating cycle is called the loop.
  c030 ov-11 b1 (21) Step three is the tools and the loop. The agent can do things, like search a file or save a note.
  c031 ov-11 b2 (14) Each of these abilities is called a tool. Record every round of the loop.
  c032 ov-11 b2 (19) Then you can trace what happened. Move on when what still fails is the model not knowing your field.
  c033 ov-12 b1 (12) Of the three steps, step three is the one most often skipped.
  c034 ov-12 b1 (18) Teams see that the context is good, but the agent still fails. So they jump straight to training.
  c035 ov-13 b1 (14) Published work kept the task and the model fixed, and changed only the loop.
  c036 ov-13 b1 (15) The agent made different decisions. That happened even when the success rate looked the same.
  c037 ov-13 b2 (19) For tasks that are mostly about finding information, the loop can matter more than how the search is built.
  c038 ov-13 b2 (16) Most teams who think they need a trained model need better tools and a better loop.
  c039 ov-finetune b2 (22) That was Deck A. Now we turn to changing the model itself. One way is to show the model thousands of examples.
  c040 ov-finetune b2 (22) Each example is a task paired with a good answer. The model learns to behave like the examples. This is called fine-tuning.
  c041 ov-pretrain b1 (20) There is a second way to teach a model. A model first learns by reading a huge amount of text.
  c042 ov-pretrain b2 (24) You can let it keep reading, using your own documents. It learns the words and ideas of your field. This is called continued pretraining.
  c043 ov-14 b1 (22) Now put the two side by side. Teaching a model how to behave and teaching it about your field are different jobs.
  c044 ov-14 b2 (17) Fine-tuning learns from thousands of examples. Continued pretraining learns from raw text. That just means plain documents.
  c045 ov-14 b3 (14) It needs billions of small pieces of words. Fine-tuning changes how the model behaves.
  c046 ov-14 b4 (21) Continued pretraining changes what it knows, and how it reads your language. So fine-tuning is the wrong tool for adding knowledge.
  c047 ov-14 b4 (12) And continued pretraining is the wrong tool for fixing format or tone.
  c048 ov-15 b1 (18) This matters for most companies. A company with lots of its own documents has plenty of raw text.
  c049 ov-15 b1 (17) But it often has very few example pairs. That makes continued pretraining the natural option for it.
  c050 ov-15 b2 (17) It also explains why so many fine-tuning projects disappoint. The wrong tool was picked for the problem.
  c051 ov-16 b1 (17) One more point about both methods. Some facts are about one customer, one case or one policy.
  c052 ov-16 b1 (20) Those facts belong in neither method. They stay in documents the agent looks up, and in the agent's governed memory.
  c053 ov-16 b2 (21) There, each fact can be shown with its source, updated or deleted. A trained model cannot do any of those things.
  c054 ov-17 b1 (20) We saw two different ways to teach a model. Now, how to choose among the five changes to the model.
  c055 ov-17 b2 (19) These five do not run in order. You look at what is failing, and pick the change that fits.
  c056 ov-17 b2 (12) You may need more than one. We call this group Deck B.
  c057 ov-adapters b1 (10) The first symptom is wrong format, tone or task behaviour.
  c058 ov-adapters b2 (18) The fix is to teach new behaviour with fine-tuning. You do not have to change the whole model.
  c059 ov-adapters b2 (18) You can train a small add-on that sits on top of it. This add-on is called an adapter.
  c060 ov-distill b1 (18) The second symptom is different. The answers are good, but the model is too slow or too expensive.
  c061 ov-distill b2 (19) Then a large model can teach a smaller, cheaper model to give the same answers. This is called distillation.
  c062 ov-reinforce b1 (20) The third symptom is right format, but wrong judgement. The answer looks right, but the decision in it is wrong.
  c063 ov-reinforce b2 (10) This fix needs results that can be checked and scored.
  c064 ov-reinforce b2 (18) The model tries, gets a score, and does more of what scored well. This is called reinforcement fine-tuning.
  c065 ov-read b1 (19) The fourth symptom is about understanding. The model does not understand the words, documents or symbols of your field.
  c066 ov-read b2 (16) We met this fix earlier. Let the model keep reading your own text with continued pretraining.
  c067 ov-custom b1 (16) The last symptom is about the data itself. Sometimes your data is not text at all.
  c068 ov-custom b1 (14) It might be a web of connections, long sequences, or signals, in huge amounts.
  c069 ov-custom b2 (18) Then look at a model built specially for that kind of data. This is called a custom architecture.
  c070 ov-18 b1 (18) Here is the whole map in one place. Wrong format, tone or behaviour points to adapters and fine-tuning.
  c071 ov-18 b3 (14) Too slow or too costly points to distillation. Wrong judgement points to reinforcement fine-tuning.
  c072 ov-18 b5 (16) Misreading your field points to continued pretraining. Data that is not text points to custom architecture.
  c073 ov-19 b1 (22) These options often combine. A standard pairing is continued pretraining, then fine-tuning. The first teaches the field. The second teaches the task.
  c074 ov-19 b2 (18) Distillation can come after any of the others. It makes the final model smaller and cheaper to run.
  c075 ov-20 b2 (23) That covers all eight changes. Now, the one thing every change depends on. Every decision to move on is based on a measurement.
  c076 ov-20 b2 (23) So you need a fixed set of real cases with known correct answers. You own it, and you test every change against it.
  c077 ov-20 b3 (21) This is called an evaluation suite. Without one, teams pick changes based on stories. And stories favour whatever is most fashionable.
  c078 ov-21 b3 (22) Here is how to build that suite. Build it from real failures, not imagined ones. Build it before the agent, not after.
  c079 ov-21 b4 (15) Have experts in your field own it. They know what a correct answer looks like.
  c080 ov-22 b1 (22) Some agents keep a memory from one task to the next. If yours does, the suite also tests what memory can break.
  c081 ov-22 b2 (14) Test a fact that changed over time. Test a question the record cannot answer.
  c082 ov-22 b3 (17) Test a record that was deleted. And test a person who is not allowed to see it.
  c083 ov-23 b1 (19) One warning belongs here. Sometimes a score comes out higher than a careful person gets on the same task.
  c084 ov-23 b2 (17) That means the system learned to score well without doing the job. This is called reward hacking.
  c085 ov-23 b2 (15) Treat it as an alarm. Check how the score is given before you trust it.
  c086 ov-24 b1 (16) Before we finish, a note on the evidence. Deck A rests partly on our own measurements.
  c087 ov-24 b2 (14) Deck B comes from published work, and from the companies that make the models.
  c088 ov-24 b3 (11) Splitting the eight changes into two decks is my own view.
  c089 ov-recap b2 (22) Here is what to remember. First, change the system before you change the model. Deck A runs in order and costs little.
  c090 ov-recap b3 (18) The evidence says most of the gain is still there. Deck B is picked by what is failing.
  c091 ov-recap b4 (20) It costs more, and it adds a model you must look after. Teaching behaviour and teaching knowledge are different jobs.
  c092 CAMERA (22) They need different kinds of data. Measure every change against real cases with known answers. The next video is Step 1, Instructions.
